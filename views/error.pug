extends layout

block css
  style
    include ../public/stylesheets/error.css


block content

  .container
    .row
      .col.text-center(style = "padding-top: 15%; padding-bottom: 13%;")
        .jumbotron(style = "background-color: white;")
          h1.display-5 Oh No!

          h3 Nothing's Here - Click Back to go Back to where you left off!

  script.
    var timer = setTimeout(function () {
      window.location.href = '/'
    }, 10000);
